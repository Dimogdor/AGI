# dépendances & artefacts de build (régénérés par `npm run build`)
node_modules/
www/
dist-pc/
dist-ota/
dist-apk/

# projets natifs générés localement (créés par `npx cap add …`,
# nécessitent Xcode / Android Studio — non versionnés ici)
android/
ios/

# divers
.DS_Store
*.log
.env

# secrets de signature Android — NE JAMAIS committer (perte = ne plus jamais
# pouvoir publier de mise à jour de l'app sous le même appId)
android-signing/
